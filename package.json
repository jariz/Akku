{
  "name": "Akku",
  "version": "0.1.0-beta.11",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release:beta": "node -r dotenv/config node_modules/.bin/release-it --preRelease=beta -n",
    "release": "node -r dotenv/config node_modules/.bin/release-it -n"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "appdmg": "^0.6.0",
    "dotenv": "^8.2.0",
    "release-it": "^12.4.3"
  }
}
